[pytest]
# loadgroup keeps @pytest.mark.xdist_group tests on one worker: the ones that
# launch a real Chromium must not run four at a time.
addopts = -p no:cacheprovider --dist loadgroup -o faulthandler_timeout=120
markers =
    xdist_group(name): run these on one xdist worker
